<div class="float-end">
    <a href="{{ $object->adminUrl }}" data-bs-toggle="tooltip" data-bs-title="Edit {{ $title }}">
        <!-- Uses default "crown" if the "pen" fontawesome icon isn't available. -->
        <i class="fas fa-pen fa-crown"></i>
        <span class="visually-hidden">Edit {{ $title }}</span>
    </a>
</div>
